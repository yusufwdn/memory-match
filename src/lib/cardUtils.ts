/**
 * cardUtils contains pure functions for creating and shuffling cards.
 *
 * "Pure function" means: given the same input, always returns the same
 * output, and has no side effects (doesn't change anything outside itself).
 *
 * These functions have no dependency on React. They are easy to test
 * and easy to reason about in isolation.
 *
 * Phase 2 will implement:
 *   - generateCards(difficulty): creates a shuffled deck for the board
 *   - shuffleArray(arr): Fisher-Yates shuffle algorithm
 */
